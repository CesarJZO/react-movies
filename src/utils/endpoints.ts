const apiURL = import.meta.env.VITE_API_URL;

/**
 * The URL of the API endpoint for the genres' controller.
 */
export const urlGenres = `${apiURL}/api/genres`;

/**
 * The URL of the API endpoint for the actors' controller.
 */
export const urlActors = `${apiURL}/api/actors`;
